#include "PH_Map.h"

namespace PhosphorEngine {

	namespace Containers {
		// TODO: FLAT MAP
		/*
		template<typename K, typename V>
		void PH_FlatMap<K, V>::Resize(uint64 size)
		{
			if (size <= inserted_elements) return; 
			auto storage_temp_save = std::move(storage); 
			for(auto)
		}

		template<typename K, typename V>
		bool PH_FlatMap<K, V>::Insert(K key, V value)
		{
			K key, V value
		};

		template<typename K, typename V>
		bool PH_FlatMap<K, V>::InsertElement_Internal(K key, V value)
		{
			for (int tries = 0; tries < max_tries; tries++)
			{
				if(storage.at(Hash(key)))
			}
		};
		*/
	};

};